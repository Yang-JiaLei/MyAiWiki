---
title: 当前Lint报告 2026-04-15
created: 2026-04-15
updated: 2026-04-15
type: report
category: wiki-ops
tags: [lint, report, cleanup]
---

# 当前Lint报告 2026-04-15

## 结论

目录结构已经重新收口，误同步回来的平行目录和错位页面已清理。

当前优先级最高的问题已经从“结构收口”转为“少量残余页面清理和语义归位”。

## P1 问题

### 1. 残余旧写法主要出现在内嵌样例块

- 当前多数正式页面的 frontmatter 已统一
- 剩余命中多数来自文档中的示例代码块或嵌入报告章节，而不是页面头部本身

主要位置：

- `Agent产出/3. Skills/install-hermes-skill-from-github.md`
- `Agent产出/1. 研究资料/hermes_agent_research/hermes_agent_research.md`

处理策略：

- 这些保留为示例内容即可
- 不再按 frontmatter 脏页处理

### 2. `type` 枚举不统一

正式页里仍需关注的旧值：

- `meta`

本轮已修复：

- `theme -> topic`
- `tool -> entity`

建议统一为：

- `index`
- `entity`
- `topic`
- `source`
- `rule`
- `workflow`
- `schema`
- `checklist`
- `ledger`
- `report`
- `template`
- `standard`
- `schedule`

### 3. 少量规范页仍保留示例正文

疑似页面：

- `个人存货/2. 学习笔记/2.2 参考文档/指南.md`
- `个人存货/2. 学习笔记/2.1 账户配置/指南.md`

这些页面现在已经被降级为“规范/指南”类页面，其中一部分也已迁入运维层；正文里的示例模板可保留，但不再视为结构性问题。

## P2 问题

### 4. 文件名可读性已基本修复

- 微信文章随机串文件已重命名
- 后续新增页面仍需遵守命名规范，避免再次产生随机文件名

### 5. 分类语义仍有待复核

- `Wiki百科/2. 主题/2.4 领域知识/Gemini.md`
  当前已修为 `type: topic`，但它是否应作为主题页还是模型实体页，仍需后续判断。

- `Wiki百科/1. 实体/1.3 产品/Hermes Agent.md`
  结构上已归位，但语义上仍可能与 `个人存货/1. 项目/1.2 Hermes Agent.md` 有重叠，需要后续拆分“外部对象页”和“我的项目页”。

## 本轮已完成

1. 清理误同步回来的平行目录
2. 删除回流的错位副本和重复页
3. 修复三批核心页面的 `title / type / category`
4. 修复 `Gemini` 与 `gemini-cli` 的类型定义
5. 统一 Prompt 库、Skills 文档和研究资料子页的 frontmatter
6. 修复 `baoyu-skills`、主研究报告、工具目录和随机文件名页
7. 将运维性质页面迁入运维层，并拆分 `Hermes Agent` 的对象页与项目记录页

## 建议处理顺序

1. 继续做语义级 lint，而不是结构级 lint
2. 处理 `Hermes Agent` 项目记录页的后续真实使用内容
3. 按需要继续补对象页与项目页之间的双向链接
